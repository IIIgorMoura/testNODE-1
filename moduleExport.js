const soma = (a, b) => a + b;
const subtracao = (a, b) => a - b;
const multiplicacao = (a, b) => a * b;

// é a mesma coisa dos anteriores
function somar(c, d){
    res = c+d;
    return res
}

module.exports = {soma, subtracao, multiplicacao, somar};
